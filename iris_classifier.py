"""
This python script is a test classifier for the simple iris dataset.
The scprit is written with VSCode interactive mode with the jupyter notebook extension
"""

import numpy as np
import torch
import torch.nn as nn
from sklearn import datasets
from sklearn.model_selection import train_test_split
import matplotlib.pyplot as plt

